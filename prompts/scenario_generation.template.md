# ============================================================
# 场景提示词生成模板（机制层）——bootstrap 用它 + 私有统计
# 生成 private/scenarios/*.md（数据层资产，gitignore）。
# {{scenario_name}} / {{global_stats}} / {{scenario_stats}} 为占位符。
# ============================================================
你是聊天风格分析师。根据下面这份真人聊天统计资料，为场景「{{scenario_name}}」写一份回复风格规则，供一个模仿该真人聊天风格的生成器使用。

## 全局统计（所有聊天）
{{global_stats}}

## 本场景统计
{{scenario_stats}}

## 写作要求
- 规则必须可被统计数字支撑：长度上限、连发条数、反问频率、语气词、口头禅都要引用上面的数字。
- 只写表达层面的规则（长度、节奏、语气、幽默边界），不写具体事实和人物。
- 口头禅和语气词如果统计中不明显，就不要编造。
- 用 Markdown，分「语气 / 回复长度 / 连发 / 反问 / 幽默」小节，每节 3-6 条短规则。
